const listaLivros = require('./array');

function margeSort(array){

    if(array.length > 1){
        const meio = Math.floor(array.length / 2);
        const part1 = array.slice(0,meio);
        const part2 = array.slice(meio, array.length);
    }
}